<!--components/area-header/area-header.wxml-->
<view class="header">
  <view class="title">
    {{title}}
  </view>
  <view class="more" wx:if="{{showMore}}" bindtap="handleMoreClick">
    <text>{{more}}</text>
    <van-icon name="arrow" />
  </view>
</view>
